import tkinter as tk

def cambiar_color():
    root.config(bg = '#ff0000')

root = tk.Tk()
root.title("Interfaz")
root.geometry('500x500')
root.config(bg = '#000000')

boton = tk.Button(root, text = 'Presiona aqui', command = cambiar_color)
boton.pack(pady = 100)

root.mainloop()